/**
 * Post-login onboarding uses the same CredNova application as /credit-ai.
 * @deprecated Import CreditAIApplyPage directly; this re-export remains for compatibility.
 */
export { default } from "./creditai/CreditAIApplyPage";
