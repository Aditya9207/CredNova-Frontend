/**
 * ProfileForm — placeholder for a future user profile management UI.
 *
 * FE-2 fix: this file previously had no content, making it confusing.
 * The /profile route currently redirects to CreditAIApplyPage (see App.tsx).
 * When a dedicated profile editor is needed, implement it here and update
 * App.tsx to use <ProfileForm /> for the /profile route.
 */

export default function ProfileForm() {
  return (
    <div style={{ padding: "2rem", textAlign: "center", color: "#6b7a90" }}>
      <p>Profile management UI — coming soon.</p>
    </div>
  );
}
